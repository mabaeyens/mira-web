# Backlog

## Done
- [2026-08-24] **The four Aura custom product pages are now live on App Store Connect** (created via the ASC API with the aura-release key; nothing committed to this repo — the record lives in the gitignored `notes/aura-custom-product-page.md` §10). Reused the empty `CPP Aura 1` shell for **Widgets** and created three more — **Avisos oficiales (CAP)**, **Apple Watch**, **Datos oficiales AEMET** — each `PREPARE_FOR_SUBMISSION`, both locales (es-ES + en-US), with promotional text set (≤170; ES Widgets copy is the finalized mockup line, the rest drafted from the angle briefs). **No screenshots, nothing submitted** — the living-sky art stays parked. Also nailed down the keyword mechanism the hard way: CPP keywords are **checkboxes drawn from the app's approved keyword field**, not free text (the ASC API exposes no writable keyword field on a CPP; `POST /v1/searchKeywords` 404s), so angle terms (widget/avisos/radar/reloj) must be added to the **app** first — which is version metadata, locked on the live 1.0.0, so it needs a new version + review. Handed that off to **aura-apps** (`docs/APP_STORE_SUBMISSION.md` + a Pending backlog entry, commit 7fe61d2) so the next release folds in the expanded 96-char keyword list and the per-page checkbox selections. Confirmed CPPs don't rotate (default listing is the generic fallback; rotation would be PPO) and app-preview = video / screenshot = image.
- [2026-08-24] **Aura CPP widget board: size widgets by real Home-Screen footprint, not fixed height** (commit 4743a79). The `ASC-widgets` board on `aura/cpp/index.html` pinned every widget `img` to `height: 208px`, so rendered width was driven by the crop's aspect ratio alone — the iPhone Large (near-square 591×620 crop) came out *narrower* than Small, and iPad XL matched Medium, collapsing the real S < M < L < XL order. Replaced the fixed height with a grid-unit model: `--u: 58px` on `.wgrid` (33px on mobile) and per-size width classes `.wg-s` 2u / `.wg-m` 4u / `.wg-l` 4u / `.wg-xl` 8u on each figure, with `height: auto` so it follows the source aspect ratio. Result: Small 116², Medium 232×108, Large 232×243 (tallest), XL 464×221 (widest) — bottoms still aligned via `align-items: flex-end`. The 640px breakpoint now just shrinks `--u` instead of re-pinning a height.
- [2026-08-24] **Aura Custom Product Page mockup is now a live page at `askmira.es/aura/cpp`** (`aura/cpp/index.html`, `noindex`, not linked from the site — reachable only by URL). Replaces the base64 `notes/aura-cpp-mockup.html` artifact with a real, self-contained page on the shared Aura design system. An iPhone frame renders the App Store product page (real chrome — icon, name, OBTENER, rating/rank meta, "Vista previa · Custom", promo text) with a scroll rail of five living-sky marketing panels (CSS-gradient skies per the note's GPT-Image-2 colour specs, each with a real screenshot bleeding off the bottom, CARROT-style). Two showcase boards below: **Screen 1** the colour Home Screen widgets from `ASC-widgets` (one widget across clear / city / rain / sunset and every size), **Screen 2** the Apple Watch complications from `ASC-complications` (rectangular / circular / corner / inline families, **colour renders only** — no greyscale lockscreen). A closing strip names the three build layers **Art / App / Code** (originally shipped as "AI · paid / Real / Code" — renamed after review, and the commit carrying the old label was rewritten out of history and force-pushed). Assets self-contained under `aura/cpp/assets/{widgets,comps,shots}/`. Commit 12bc8da.
- [2026-08-24] **Aura gallery refreshed from the App Store Connect (ASC-*) materials; colour-only widgets** (commit dd5f686). Re-exported every gallery screenshot in `aura/index.html` from the latest `../aura-apps/screenshots/ASC-{iphone,ipad,watch}` sources (sips + cwebp into `aura/assets/shots/`). Added three new Apple Watch faces — compass (wf3), chronograph pro (wf4), chronograph gauges (wf5); the watch group note now reads "complications across five faces". Rebuilt the **Widgets** gallery: the old grid mixed colour chips with greyscale *lockscreen* complication renders, so I replaced it with six full-condition **colour** Home Screen widget renders (iPhone S/M/L, iPad M/L/XL across clear / city / rain / sunset) in a new height-normalised `.rail-widget` scroll rail that keeps each size's true proportions. Deleted the 12 orphaned lockscreen/complication widget webps. Skipped `ASC-ipad/07_hero_clear_dusk` — the iPad row already has a dusk hero and its 1.45:1 aspect doesn't fit the 4:3 frame without cropping the sun.
- [2026-08-24] **Aura pages: AEMET key request button + accurate data-sharing in the privacy policy** (commit f6c92a0). **Soporte** (`aura/soporte/index.html`): added a prominent "Request an AEMET API key" button card (new `#ico-key` symbol, links to AEMET's `centrodedescargas/altaUsuario`) and turned the FAQ's plain `opendata.aemet.es` text into a real link — bilingual, reusing `.support-mail`/`.support-card`. **Privacidad** (`aura/privacidad/index.html`): after verifying against the real app code (via the aura-apps peer session), corrected two inaccurate claims — the hourly UV source is **Open-Meteo** (`air-quality-api.open-meteo.com`, serving CAMS/Copernicus data), *not* Copernicus directly, and it receives the **municipality centroid coordinates**, not the INE code. Listed Open-Meteo as the one non-Spanish-official endpoint; kept the "Spain's official public data services" summary line but appended "and public open-data sources" with a `*` footnote anchoring to the CAMS list item. Added a **payload snippet** (new "What actually leaves your device" section, bilingual, styled `pre.payload`) showing the exact user data each request sends — per Miguel's simplification: one comment + one code line each (AEMET INE ID `28079` "not a zip code", MITECO station code `28079004#…`, Open-Meteo `latitude=XX&longitude=YY` "a point near you, not your real position"). Also linked the contact section to Soporte, and applied Spanish copy edits ("servidores propios", "nunca llama", "si permites el acceso", dropped "nunca hasta mí").
- [2026-08-24] **Added a gitignored `notes/` folder** for internal discussion documents (alongside the existing gitignored `specs/`), with a README explaining the split: `notes/` holds thinking/decisions/drafts, `specs/` holds concrete 5-bullet work items. Only `.gitignore` changed in the public repo. Used it to draft the **Aura App Store custom product page (CPP) plan** — see the Pending item below. The plan and its live mockup live in `notes/` and never ship to the site.
- [2026-08-23] **Aura support page at `askmira.es/aura/soporte`** (fixes App Store rejection, Guideline 1.5). New `aura/soporte/index.html` on the Aura design system (shared `style.css` + `aura/style.css`, no Tailwind, shared `mira-theme` toggle), a real support page to replace the rejected `github.com/mabaeyens/aura-apps/issues` URL (an empty issues list is not a support page). Contact email dears-respect.8u@icloud.com plus GitHub Issues as secondary, and an 8-item FAQ. Made **bilingual (English / Spanish)** with a globe language selector in the header, placed between the back button and GitHub; the choice is remembered in `localStorage` (`aura-lang`) and defaults to Spanish for es-* browsers. Corrected the AEMET FAQ answer after review feedback: the API key is required, not bundled (AEMETClient throws missingAPIKey when empty). The `/aura` marketing page footer now links to it ("Support"). Deployed via GitHub Pages from main root (CNAME askmira.es); commits 34b4b8f, f025cc5, 6f719bd. This URL is what goes in the App Store Connect Support URL field.
- [2026-08-22] **Aura page: real screenshots + copy refresh, dropped Mac.** Replaced the four dashed `.shot` placeholders with a real hero teaser (three iPhone Hoy shots — clear / storm / night, showing the living sky) and a full **gallery section** ("Every surface, the same living sky") with CSS scroll-snap rails per device: iPhone (8), iPad (7), Apple Watch (4 app + 10 face complications), and a widgets grid (12). Sources: `aura-apps/screenshots/{iphone,ipad,watch,widget-previews}`; the originals are 4–7 MB PNGs each (~85 MB), re-encoded to WebP at web sizes into `aura/assets/shots/` (**1.2 MB total**) via `sips` + `cwebp`. Added the missing `aura/assets/og.png` (1200×630, cropped from the iPad hero). **Removed Mac as a surface** per its actual state — it runs on Apple Silicon as "Designed for iPad", so the eyebrow/hero-note now read iPhone · iPad · Apple Watch, "Mac in the works" is gone, and the iPad group carries a small "also runs on Apple Silicon Macs as Designed for iPad" note. Copy pulled up to the shipped v1.0.0 feature set (editorial on-device Spanish hero, hourly/7-day, sun→sunset arc, wind, air quality MITECO ICA, UV, radar, AEMET bulletin; iPad extra-large widget). App Store Connect custom product page still to come. Gallery styles added to `aura/style.css`; placeholder styles removed.
- [2026-07-10] **Vera subpage redesigned to match** the new hybrid design: now links the shared root `style.css` plus a small `vera/style.css` that only swaps the accent to Vera teal and keeps `.repo-chip`. Tailwind reference removed from `/` and `/vera` — but `vera/privacy/index.html` still links `../../assets/tailwind.css`, so keep the Tailwind files until that page is migrated to the shared design system. vera-apps is **public** now: added "Download for Mac" (→ `vera-apps/releases/latest`, verified `Vera-1.2.0.dmg` present) and "View source" links (header, hero, footer); dropped "Mac coming soon" and "free during testing" (release copy says free & open source); GitHub-native card updated to one-tap OAuth + branch switching + multi-file atomic commits (v1.2.0 notes). TestFlight public link `AyS6RfM8` verified live.
- [2026-07-10] **Full redesign of the main page** (hybrid direction: Apple-clean structure with field-manual accents). Natural document scroll everywhere (scroll-snap and dot-nav removed), centered hero with the live-demo panel as centerpiece (traffic-light header added), segmented-control device tabs, 3×2 capability grid, 2-col performance layout, reveal-on-scroll animations (reduced-motion safe). All JS behaviors ported: live conversation replay, bench bars, carousel + lightbox + keyboard nav, theme toggle. **Tailwind dropped from the main page** — `style.css` is now fully handwritten (tokens unchanged, still mirror the app palette). `assets/tailwind.css` kept only because `/vera` still links it; the rebuild step no longer applies to `index.html`.
- [2026-06-14] Fixed mobile/tablet rendering. Replaced the runtime Tailwind **CDN** (`cdn.tailwindcss.com`) with a **vendored, purged** `assets/tailwind.css` so layout no longer depends on a third-party script — it was collapsing to a bare logo on iPad under a content blocker / Lockdown Mode. Also made the layout responsive: natural document scroll on mobile (scroll-snap kept desktop-only), wrapping meta rows, dot-nav hidden on phones, icon-only nav chips on narrow screens, and `img { max-width:100% }` overflow guards.
- [2026-06-14] Promoted the "field-manual" redesign to the live site (`index.html`; old design kept in git history). Scroll-snap field-manual layout, containment hairline frame, numbered section labels (`01 /`…), capability index numerals, LinkedIn links in nav + footer, and a rebuilt hero.
- [2026-06-14] Hero "local generation" panel now replays four **real** Mira conversations as an auto-scrolling chat, mirroring the app UI: extended **thinking** (Thinking…→Thought), **web sources** (Pages read), **RAG** (Document sections used + scores), and **code** (language label + Copy). Live tok/s readout; `prefers-reduced-motion` renders a single static scene.

- [2026-08-20] Added the **Aura subpage** at `askmira.es/aura`. Same lean single-page pattern as Vera (`aura/index.html` + `aura/style.css` + `aura/assets/icon.png`): header → hero → screenshot placeholders → 6 feature cards → footer, positioned as "a personal, private weather app for Spain, on every Apple surface" (iPhone/iPad/Mac/Watch, powered by AEMET OpenData + my own free key). Aura **sky-blue** accent pulled from the app icon (dark `#4FA3F0` / light `#1466C4`), plus a warm `--sun` for the icon's corner glow; reuses the Mira design system and shares the `mira-theme` toggle key. CTAs are "View source" (`aura-apps`, public/MIT) and "Get an AEMET key" (opendata.aemet.es alta) — no TestFlight/DMG yet (iOS + widgets + Watch working, Mac in the works). **Screenshots are placeholders** (`.shot` dashed tiles: iPhone/Widgets/Watch/Lock Screen) — swap for real shots later and add `aura/assets/og.png`. Copy pulled verbatim from `aura-apps/README.md`. One ecosystem cross-link added to the main-page footer.
- [2026-06-22] Added the **Vera subpage** at `askmira.es/vera` (`a3e8b29`, icon fix `d8cdd8d`). Lean single-page in this same repo (`vera/index.html` + `vera/style.css` + `vera/assets/`): nav → hero → 4 feature cards → footer, positioned as "the native Apple app for the Markdown in your repos". Vera **brand teal** accent (dark `#4FC9CE` / light `#0B7C7E`); reuses the Mira design system (Lora + IBM Plex Mono, grain) and shares the `mira-theme` toggle key. TestFlight beta CTA (`AyS6RfM8`, "free during testing"); Mac download marked "coming soon". No source link / MIT / "free forever" (vera-apps is private). `vera/assets/icon.png` = real app icon (`AppIcon-1024.png`, resized 512). `tailwind.config.js` content now includes `./vera/index.html`; one ecosystem cross-link added to the Mira credits section.

- **Aura CPP — art generation (the last parked piece).** The four pages exist on ASC with promo text + a keyword plan (see Done above); what's still parked is the **screenshots**. GPT Image 2 generates only the **living-sky backgrounds** (6 tuned, reusable prompts — no text/UI/device, so no paid re-runs); the **real** Aura screenshots go in the device frames and headlines are composited by code. Locked: **planning only** (no generation until I say go — cost is real), **Spanish + English** (skies are text-free, so EN+ES adds no extra generations). Still to decide: final panel list, 2K vs 4K, and how the OpenAI key reaches the pipeline. Est. ~$0.30–0.50 for the iPhone sky set. Once art exists: composite → attach per locale → submit each CPP. Keyword follow-up is tracked in aura-apps (`docs/APP_STORE_SUBMISSION.md`), applied at the next app release. Full detail in the gitignored `notes/aura-custom-product-page.md`.
- **Vera launch — remaining items are specced in the (gitignored) `specs/` folder:** Mac download mechanism (`specs/01`), screenshots + `og.png` (`specs/02`), monetization/StoreKit Pro unlock (`specs/03`), reconcile vera-apps docs (`specs/04`). Several need a decision or assets from Miguel.
- Browser QA of the new live design: scene pacing/legibility, hero panel fits within 100vh without clipping, mobile stacking (text over panel), dark/light across thinking/sources/RAG/code, reduced-motion path.
- Restore TestFlight buttons once Apple external review approves the URL; add back at nav, hero CTA, and credits (3 spots)
- Consider a waitlist CTA (Buttondown recommended for GDPR/EU) once TestFlight is live or as a pre-launch alternative

## Notes
- Design direction is deliberately "field manual for a machine you own" (mono/spec-sheet, warm stone + terracotta, honest copy) — chosen over a generic Raycast/Arc/Linear SaaS template to stay distinctive.
- Hero demo content is pulled **verbatim (trimmed)** from real conversations in `~/.local/share/mira/conversations.db` (non-sensitive: black holes, Madrid weather via aemet.es, Apple Silicon brief summary, palindrome). Only the RAG source-chip scores/snippets are representative — Mira doesn't persist retrieved chunks; prompts/thinking/answers are real. Personal/Vera conversations were deliberately avoided.
- Mira's SwiftUI app palette is identical to the site's CSS `:root` tokens (bg `#1C1917`, accent `#D09268`, muted `#A8A29E`, surface `#232120`, border `#57534E`), so the existing tokens reproduce the app's look — no new colors needed.
- CSS: page-specific component CSS/JS still lives inline in `index.html`; shared structural CSS is in `style.css`. Tailwind utilities are compiled to `assets/tailwind.css` (committed). **Rebuild after changing any Tailwind class in the markup:** `npx tailwindcss@3 -c tailwind.config.js -i tailwind-input.css -o assets/tailwind.css --minify` (config in `tailwind.config.js`, input in `tailwind-input.css`). No CDN at runtime.
